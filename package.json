{
  "name": "lambda-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc app.ts && tsc app/config/db.config.ts && tsc app/models/index.ts && tsc app/models/model.ts && tsc app/routes/record.ts",
    "start": "npm run transpile && node app.js local",
    "deploy": "npm run transpile && serverless deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^4.11.0",
    "mongoose": "^6.7.0",
    "serverless-http": "^3.0.2",
    "typescript": "^4.8.4"
  }
}
